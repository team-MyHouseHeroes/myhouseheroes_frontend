// src/styles/Write/WriteHeroScreenStyles.ts

import {StyleSheet} from 'react-native';

const WriteHeroScreenStyles = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
  },
  text: {
    fontSize: 20,
  },
});

export default WriteHeroScreenStyles;
